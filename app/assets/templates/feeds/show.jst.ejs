<h3><%= feed.escape('title') %></h3>
<ul>
  <% entries.each(function(entry) { %>
    <li>
      <a href="#/entries/<%= entry.id %>"><%= entry.escape('title') %></a>
    </li>
  <% })%>
</ul>
<button class='feed-refresh'>Refresh Me</button>
